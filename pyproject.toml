[project]
name = "metric_registry"
version = "0.1.0"
description = "Dynamic metric registry for VLMHyperBench"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv>=0.9.26"]
build-backend = "uv.build"